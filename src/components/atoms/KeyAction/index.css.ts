import { style } from '@vanilla-extract/css';

export const container = style({
  display: 'inline-block',
  backgroundColor: 'rgba(0 0 0 / .05)',
  boxShadow: '1px 1px 3px -0.5px rgba(0 0 0 / .5)',
  borderRadius: 8,
  padding: '.75rem',
})
